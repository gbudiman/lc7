PROGRAM simpleexpr
BEGIN
	STRING eol := "\n";
	INT a,b,c,d;
	FLOAT x,y,z,t;
	STRING tab := "\t";

        --PROTO VOID main();

	FUNCTION VOID main()
	BEGIN
		a := 1;
		b := 2;
		c := 10;
		d := 20;
		a := a + b;
		WRITE (a,eol);
		b := a * c;
		WRITE (b,eol);
		c := 0 - a + b;
		WRITE (c,eol);
		d := 0 - d;
		WRITE (d,eol);
		a := a + 10;
		WRITE (a,eol);
		b := b + a + 10;
		WRITE (b,eol);
		c := 0 - 10;
		WRITE (c,eol);
		x := 1.0;
		y := 2.0;
		z := 3.14159;
		x := z+2.0-x;
		y := z/y;
		WRITE (x,eol);
		WRITE (y,eol);
	END	
END

